import DashboardSection from "@/components/dashboard";
import Layout from "@/components/Layout";

const Home = () => {
  return (
    <Layout>
      <DashboardSection />
    </Layout>
  );
};

export default Home;
