import HomeSection from "@/components/home";
import Layout from "@/components/Layout";

const HomePage = () => {
  return (
    <Layout>
      <HomeSection />
    </Layout>
  );
};

export default HomePage;
